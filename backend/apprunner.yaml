version: 1.0
runtime: docker
run:
  command: uvicorn api:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
  env:
    - name: PORT
      value: "8000"
    - name: PYTHONPATH
      value: "/app"
